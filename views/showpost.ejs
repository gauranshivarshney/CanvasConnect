<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PostWave</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h screen bg-custom-pink p-10">
        <div class="posts text-black">
            <h3 class="text-black-400 font-bold text-2xl">Your Posts</h3>
            <div class="p-7 text-black">
                <div class="posts p-5 flex gap-3 flex-wrap">
                    <% if(posts.length > 0) { %>
                        <% posts.forEach(function(user){ %>
                            <div class="details w-72 p-3 bg-zinc-400 rounded-lg">
                                <p class="text-2xl mt-2 text-black-900 font-bold"><%= user.title %></p>
                                <p><img src="/images/uploads/<%= user.image %>" alt="Post Image" class="mt-2 w-full h-auto"></p>
                                <hr class="border-white-900">
                                <p class="text-xl mt-3 text-white"><%= user.content %></p>
                                <div class="flex gap-12 mt-10">
                                    <a class="text-yellow-600 font-bold" href="/edit/<%= user._id %>">Edit</a>
                                    <a class="text-red-600 font-bold" href="/delete/<%= user._id %>">Delete</a>
                                </div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <h3 class="text-black">No posts yet.</h3>
                    <% } %>
                </div>
            </div>
        </div>
        <a class="text-blue-800 font-bold" href="/userprofile">Go to Home</a>
    </div>
</body>
</html>